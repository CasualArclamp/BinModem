[package]
name    = "proxy"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
socks = { path = "../socks" }
tcp   = { path = "../tcp" }

[lints]
workspace = true
